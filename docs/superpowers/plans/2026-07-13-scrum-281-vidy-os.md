# SCRUM-281 «Виды ОС» Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Пикеры показывают `displayName` групп («2360 - Машины и оборудование») и фильтруют «Вид ВНА» по «Счёту учёта» текущей строки ТЧ (новый хинт `rowFilter` от бэка).

**Architecture:** Обе правки — легаси-зона (`dict-sidebar`, `form-renderer`); SDUI не трогаем. Часть 1 — однострочник в колонке пикера. Часть 2 — новый хук `useRowFilter` (зеркалит `useCellDependency`), результат вливается в существующую цепочку `mergeSearchParams` в `DictCell`; дропдаун, «Показать все» и инвалидация кэша работают без изменений, т.к. `searchParams` уже входят в queryKey.

**Tech Stack:** React 19, react-hook-form (`useWatch`), TanStack Query, vitest + @testing-library/react.

**Design doc:** `docs/superpowers/specs/2026-07-13-scrum-281-vidy-os-design.md`

## Global Constraints

- Ветка: `feat/scrum-281-vidy-os` (уже создана от `feat/sdui-scrum-265`).
- ЛЕГАСИ-таска: не трогать `src/features/sdui/`; легаси-файлы не рефакторить (минимальные правки).
- Оба изменения аддитивные: без `rowFilter`/`displayName` в JSON бэка поведение не меняется.
- af-формат фильтра: `af=AttrCode:EntryId`, несколько условий — через запятую (уже реализовано в `mergeSearchParams`).
- Обработка `rowFilter` универсальная по map (возможно >1 пары), НЕ хардкодить под VidVNA.
- `rowFilter` у атрибутов без правила в JSON отсутствует вовсе (не `null`) → тип поля опциональный.
- Не использовать `useMemo`/`useCallback` без явной перф-причины (правило проекта).
- Формат коммитов: `feat|fix|add|refactor: описание` (commit-msg hook). Warning «pre-commit hook was ignored» — ожидаем, не чинить.
- НЕ запускать полные `tsc --noEmit`/`npm run lint` по всему репо без нужды — только целевые файлы (`npx eslint <files>`); `tsc --noEmit` один раз на финале (инкрементален, ок).
- Прогонять только релевантные vitest-файлы, полную пачку — на финальной верификации.

---

### Task 1: Часть 1 — displayName в колонке «Наименование» пикера DictSidebar

**Files:**
- Modify: `src/features/dict-sidebar/ui/dict-sidebar-list-view.tsx:233`

**Interfaces:**
- Consumes: `DictEntry.displayName?: string` (уже объявлено в `src/features/dict-sidebar/api/dict-sidebar-api.ts:32-43`); `getLocalizedName(row, lang)` — уже импортирован в файле.
- Produces: ничего для других тасок (изолированная правка).

Контекст: бэк присылает у групп справочника `VidyDolgosrochnykhAktivov` `displayName = "{код счёта} - {наименование}"`. Чип выбора (`handleSelect`, line 272-284) и inline-автокомплит уже используют `displayName ?? …`; отстаёт только колонка таблицы «Показать все». Юнит-тест не пишем: легаси-файл 475 строк, извлечение accessor ради теста = запрещённый рефакторинг; приёмка руками по дизайн-доку.

- [ ] **Step 1: Правка accessorFn**

В `src/features/dict-sidebar/ui/dict-sidebar-list-view.tsx` найти (line ~231-240):

```ts
    const nameColumn: ColumnDef<DictEntry> = {
      id: 'nameRu',
      accessorFn: (row) => getLocalizedName(row, i18n.language),
```

заменить строку `accessorFn` на:

```ts
      accessorFn: (row) => row.displayName ?? getLocalizedName(row, i18n.language),
```

- [ ] **Step 2: Целевые проверки**

Run: `npx eslint src/features/dict-sidebar/ui/dict-sidebar-list-view.tsx`
Expected: 0 errors, 0 warnings (файл уже был чист).

- [ ] **Step 3: Commit**

```bash
git add src/features/dict-sidebar/ui/dict-sidebar-list-view.tsx
git commit -m "fix: displayName в колонке «Наименование» пикера DictSidebar (SCRUM-281)"
```

---

### Task 2: Тип rowFilter + хук useRowFilter (TDD)

**Files:**
- Modify: `src/entities/document-type/types/document-type.ts:46-66` (интерфейс `DocumentAttribute`)
- Create: `src/features/form-renderer/lib/hooks/use-row-filter.ts`
- Test: `src/features/form-renderer/lib/hooks/use-row-filter.test.tsx`

**Interfaces:**
- Consumes: `Control` из `react-hook-form`; shape ссылочной ячейки в form state — `{ id: number|string, ... } | null`.
- Produces (для Task 3):
  - `DocumentAttribute.rowFilter?: Record<string, string>` — ключ = af-код атрибута целевого справочника, значение = binding сестринской колонки строки;
  - `useRowFilter(rowFilter: Record<string, string> | undefined, rowPathPrefix: string, control: Control<Record<string, unknown>>): Record<string, string> | undefined` — `{ af: 'Attr:id[,Attr2:id2]' }` либо `undefined`, если слать нечего.

- [ ] **Step 1: Расширить DocumentAttribute**

В `src/entities/document-type/types/document-type.ts` в конец интерфейса `DocumentAttribute` (после `formEvent: string | null`, line 65) добавить:

```ts
  // Отбор пикера ссылочной колонки ТЧ по сестринской ячейке той же строки
  // (SCRUM-281): ключ — af-код атрибута ЦЕЛЕВОГО справочника, значение —
  // binding колонки ТЕКУЩЕЙ строки. У атрибутов без правила ключ в JSON
  // отсутствует вовсе.
  rowFilter?: Record<string, string>
```

- [ ] **Step 2: Написать падающий тест**

Создать `src/features/form-renderer/lib/hooks/use-row-filter.test.tsx`:

```tsx
import { renderHook } from '@testing-library/react'
import { describe, expect, it } from 'vitest'
import { useForm } from 'react-hook-form'

import { useRowFilter } from './use-row-filter'

// Хук читает сестринские ячейки строки ТЧ через useWatch, поэтому рендерим
// его внутри формы с предзаполненными значениями строки.
const renderRowFilter = (
  rowFilter: Record<string, string> | undefined,
  formValues: Record<string, unknown>,
) =>
  renderHook(() => {
    const { control } = useForm<Record<string, unknown>>({
      defaultValues: formValues,
    })
    return useRowFilter(rowFilter, 'OsnovnyeSredstva.0', control)
  })

describe('useRowFilter (отбор пикера по ячейке строки ТЧ)', () => {
  it('заполненная ячейка → af в формате AttrCode:id', () => {
    const { result } = renderRowFilter(
      { SchetUcheta: 'SchetUcheta' },
      {
        OsnovnyeSredstva: [
          { SchetUcheta: { id: 4711, presentation: '2360' } },
        ],
      },
    )
    expect(result.current).toEqual({ af: 'SchetUcheta:4711' })
  })

  it('пустая (null) и не-ссылочная (без id) ячейка → undefined', () => {
    const empty = renderRowFilter(
      { SchetUcheta: 'SchetUcheta' },
      { OsnovnyeSredstva: [{ SchetUcheta: null }] },
    )
    expect(empty.result.current).toBeUndefined()

    const nonRef = renderRowFilter(
      { SchetUcheta: 'SchetUcheta' },
      { OsnovnyeSredstva: [{ SchetUcheta: 'просто строка' }] },
    )
    expect(nonRef.result.current).toBeUndefined()
  })

  it('несколько пар map → af через запятую, пустые пары пропускаются', () => {
    const { result } = renderRowFilter(
      { SchetUcheta: 'SchetUcheta', Sklad: 'Sklad', MOL: 'MOL' },
      {
        OsnovnyeSredstva: [
          { SchetUcheta: { id: 1 }, Sklad: null, MOL: { id: 2 } },
        ],
      },
    )
    expect(result.current).toEqual({ af: 'SchetUcheta:1,MOL:2' })
  })

  it('rowFilter отсутствует у колонки → undefined', () => {
    const { result } = renderRowFilter(undefined, {
      OsnovnyeSredstva: [{ SchetUcheta: { id: 1 } }],
    })
    expect(result.current).toBeUndefined()
  })
})
```

- [ ] **Step 3: Убедиться, что тест падает**

Run: `npx vitest run src/features/form-renderer/lib/hooks/use-row-filter.test.tsx`
Expected: FAIL — `Cannot find module './use-row-filter'` (или аналогичная ошибка резолва).

- [ ] **Step 4: Реализовать хук**

Создать `src/features/form-renderer/lib/hooks/use-row-filter.ts`:

```ts
import { useWatch, type Control } from 'react-hook-form'

// Отбор пикера ссылочной колонки ТЧ по сестринским ячейкам ТОЙ ЖЕ строки
// (SCRUM-281, хинт rowFilter из метаданных типа-строки ТЧ). В отличие от
// useCellDependency (источник — шапка документа), источник здесь — ячейка
// текущей строки: rowPathPrefix = '<КодТЧ>.<idx>'. Метаданные не грузим —
// map уже пришёл в атрибуте колонки.
//
// Возвращает { af: 'Attr:id[,Attr2:id2]' } (формат mergeSearchParams/бэка)
// либо undefined — «фильтра нет»: пустая ячейка = полный список, мягкий
// отбор как в 1С (ПараметрыВыбора).
export const useRowFilter = (
  rowFilter: Record<string, string> | undefined,
  rowPathPrefix: string,
  control: Control<Record<string, unknown>>
): Record<string, string> | undefined => {
  const entries = Object.entries(rowFilter ?? {})

  // Ссылочная ячейка в form state — { id, ... } | null.
  const values = useWatch({
    control,
    name: entries.map(([, binding]) => `${rowPathPrefix}.${binding}`),
  }) as unknown[]

  const parts: string[] = []
  entries.forEach(([attrCode], i) => {
    const id = (values[i] as { id?: number | string } | null | undefined)?.id
    if (id != null) parts.push(`${attrCode}:${String(id)}`)
  })

  return parts.length > 0 ? { af: parts.join(',') } : undefined
}
```

- [ ] **Step 5: Убедиться, что тесты зелёные**

Run: `npx vitest run src/features/form-renderer/lib/hooks/use-row-filter.test.tsx`
Expected: PASS, 4/4.

- [ ] **Step 6: Целевые проверки**

Run: `npx eslint src/features/form-renderer/lib/hooks/use-row-filter.ts src/features/form-renderer/lib/hooks/use-row-filter.test.tsx src/entities/document-type/types/document-type.ts`
Expected: 0 errors, 0 warnings.

- [ ] **Step 7: Commit**

```bash
git add src/entities/document-type/types/document-type.ts src/features/form-renderer/lib/hooks/use-row-filter.ts src/features/form-renderer/lib/hooks/use-row-filter.test.tsx
git commit -m "feat: хук useRowFilter — отбор пикера по ячейке строки ТЧ (SCRUM-281)"
```

---

### Task 3: Встраивание useRowFilter в DictCell

**Files:**
- Modify: `src/features/form-renderer/ui/table-cell-renderer.tsx:135-144` (компонент `DictCell`)

**Interfaces:**
- Consumes (из Task 2): `useRowFilter(rowFilter, rowPathPrefix, control): Record<string, string> | undefined`; `column.rowFilter?: Record<string, string>`.
- Consumes (существующее в файле): prop `name` формата `'<КодТЧ>.<idx>.<col>'` (напр. `'OsnovnyeSredstva.0.VidVNA'`); `mergeSearchParams(a, b)` из `../lib/utils/field-filter-params` (уже импортирован); переменная `searchParams` дальше по коду уже уходит в queryKey дропдауна (line 178), в params запроса (line 182) и в `push({ searchParams })` «Показать все» (line 207).
- Produces: ничего нового наружу — только расширенный `searchParams`.

Новый юнит-тест на DictCell не пишем: интеграция — 2 выражения поверх уже протестированного хука, а обвязка DictCell (react-query, dict-sidebar store, RHF) в легаси не замокана; поведение закрывается ручной приёмкой (дизайн-док, §Приёмка). Реактивность и кэш — бесплатно: `searchParams` уже в queryKey.

- [ ] **Step 1: Подключить хук**

В `src/features/form-renderer/ui/table-cell-renderer.tsx` найти в `DictCell` (line ~135-144):

```ts
  // Dependent dictionaries (e.g. Подразделения filtered by owner org) need the
  // owner filter, which the row lacks — it is sourced from the header field.
  const { searchParams: depParams, disabled } = useCellDependency(
    column,
    control
  )

  // Фильтр поля (напр. отбор МОЛ по «Организации» документа) объединяется
  // с af-фильтром зависимости (af-условия склеиваются, не затирают друг друга).
  const searchParams = mergeSearchParams(serverFilterParams, depParams)
```

заменить на:

```ts
  // Dependent dictionaries (e.g. Подразделения filtered by owner org) need the
  // owner filter, which the row lacks — it is sourced from the header field.
  const { searchParams: depParams, disabled } = useCellDependency(
    column,
    control
  )

  // rowFilter (SCRUM-281): отбор по сестринской ячейке ТОЙ ЖЕ строки
  // (напр. «Вид ВНА» по «Счёту учёта»). name = '<КодТЧ>.<idx>.<col>' →
  // префикс строки = name без последнего сегмента.
  const rowParams = useRowFilter(
    column.rowFilter,
    name.slice(0, name.lastIndexOf('.')),
    control
  )

  // Фильтр поля (напр. отбор МОЛ по «Организации» документа) объединяется
  // с af-фильтрами зависимости и строки (af-условия склеиваются, не
  // затирают друг друга).
  const searchParams = mergeSearchParams(
    mergeSearchParams(serverFilterParams, depParams),
    rowParams
  )
```

и добавить импорт хука сразу после line 25 (`import { useCellDependency } from '../lib/hooks/use-cell-dependency'`):

```ts
import { useRowFilter } from '../lib/hooks/use-row-filter'
```

- [ ] **Step 2: Проверки**

Run: `npx vitest run src/features/form-renderer && npx eslint src/features/form-renderer/ui/table-cell-renderer.tsx`
Expected: все существующие тесты form-renderer зелёные (регрессии нет), eslint 0/0.

- [ ] **Step 3: Commit**

```bash
git add src/features/form-renderer/ui/table-cell-renderer.tsx
git commit -m "feat: rowFilter в DictCell — отбор «Вид ВНА» по «Счёту учёта» строки (SCRUM-281)"
```

---

### Task 4: Финальная верификация

**Files:** нет новых; только проверки и push.

- [ ] **Step 1: Вся пачка тестов**

Run: `npm run test -- --run` (или `npx vitest run`)
Expected: все зелёные (до правок было 142 passed: 140 + 2 из dispatch.test.ts; станет 146 с 4 новыми).

- [ ] **Step 2: Типы**

Run: `npx tsc --noEmit`
Expected: 0 ошибок.

- [ ] **Step 3: Линт изменённых файлов**

Run:
```bash
npx eslint src/features/dict-sidebar/ui/dict-sidebar-list-view.tsx src/entities/document-type/types/document-type.ts src/features/form-renderer/lib/hooks/use-row-filter.ts src/features/form-renderer/lib/hooks/use-row-filter.test.tsx src/features/form-renderer/ui/table-cell-renderer.tsx
```
Expected: 0 errors, 0 warnings. (В остальном репо есть pre-existing ошибки — их не чинить.)

- [ ] **Step 4: Push**

```bash
git push -u origin feat/scrum-281-vidy-os
```

Ручная приёмка на dev (после выкладки бэка `talgat/os-card-fields`) — по дизайн-доку §«Приёмка руками»: группы «2360 - …» в «Группе ОС»; фильтр «Вид ВНА» по счёту 2360 в «Поступлении от контрагента»; очистка счёта → полный список; колонки без rowFilter — без изменений.
