# Аудит Ф3 — группа «Шелл: сайдбар, топбар, workspace-вкладки, страница модуля»

Figma fileKey `8RxAFhNubquQ1bz912H2wB`. Ноды: 150:4094 («Банк и касса»), 314:14173/170:6391 («main menu»), 40:573 («Top menu»), 32:2950 («Logo»), 58:1107/58:1127 («Tab»/«Tab bar»), 175:7122 («Отчёты»).
Скрины сохранены в `/private/tmp/claude-501/-Users-buterbrot359-Development-MishaWeb-fin-web/4819f856-4d7e-442f-b097-9c8cb8000eaa/scratchpad/f3-audit/figma/` (shell-150-4094.png, mainmenu-314-14173.png, tab-58-1107.png, tabbar-58-1127.png, topmenu-40-573.png, reports-175-7122-full.png).

Кейс владельца: «в сайдбар-меню мрак — табы, паддинги, отступы»; на скринах пунктам тесно, актив — салатовый квадрат с иконкой + тёмная плашка на всю ширину. **Важно**: по Figma-спеке активная плашка строки — БЕЛАЯ (`#FFFFFF`), а не тёмная (см. §1.1); код (`bg-ui-01`, fallback `#ffffff`) формально тоже задаёт белую плашку. Тёмная плашка, которую видит владелец, не объясняется ни спекой, ни статическим чтением кода — нужна визуальная проверка на dev-стенде (не входила в задачу этого экстрактора), возможный источник — рантайм-переопределение CSS-переменной `--ui-01` или проблема кэша/темы, не найденная в `src/app/index.css` (там только `:root{}` без переопределений палитры).

---

## §1. Спека Figma (числа)

### 1.1 Сайдбар (150:4094 / 170:6391 «main menu»)

- Контейнер: полная высота, тёмный фон `UI 06 #222124`. Ширина видимой тёмной зоны = 412px (расстояние от края фрейма до начала `Body`/светлого контента: `Body.x=412` в узле 150:4095).
- Логотип: инстанс `Logo` (32:2950) `x=80,y=40,w=230,h=40` — иконка `Frame 2` 40×40, текст `Web-бухгалтерия` начинается на x=60 от иконки (x=60 внутри Logo, т.е. gap иконка↔текст ≈ 20px), шрифт: заголовочный (аналог H6/Body1 b).
- Список пунктов: инстанс «main menu» `x=60,y=140,w=332,h=560` → отступ от левого края тёмной зоны до пунктов = 60px, от правого края пунктов до края тёмной зоны = 412-60-332=20px.
- Один пункт меню (170:6391, компонент «Menu list», 10 инстансов по 332×56, `y` шагом ровно 56 — **зазора между строками нет**):
  - Иконка: `Menu icon` `x=20,y=8,w=40,h=40` → левый паддинг 20px, верт. паддинг (56-40)/2=8px, иконка занимает **весь** квадрат 40×40 (без доп. отступа глифа внутри).
  - Текст: начинается `x=72` → gap иконка↔текст = 72-20-40 = **12px**; `y=18` → вертикально по центру (56-20)/2=18, шрифт Body1 (16px/500, Google Sans).
  - Активный пункт: пилюля на всю ширину строки (332px), фон **`UI 01 #FFFFFF`** (белый), сильно скруглённые углы (визуально «капсула», радиус заметно больше 12px — ближе к половине высоты ~28px); квадрат иконки — **`Accent 01 #DAF449`** (салатовый), глиф иконки — тёмный (`UI 06`); текст — тёмный.
  - Дефолтный (неактивный) пункт: фон строки прозрачный (сливается с тёмным сайдбаром); квадрат иконки визуально не выделен (совпадает с фоном сайдбара `UI 06`, никакого отдельного «квадрата» не видно); глиф иконки — белый (`UI 01`); текст — белый.

### 1.2 Топбар (40:573 «Top menu», иконный кластер справа)

- Кластер `Top menu` `w=545,h=40`, стоит по правому краю контентной зоны на `y=40` от верха (т.е. верхний паддинг контента ≈40px).
- Состав слева направо: button(40×40) → divider(1×28) → button(40×40) → divider → button(40×40) → divider → button(40×40) → divider → button-профиль(300×40) → divider → button-гамбургер(40×40).
- Между всеми соседними элементами — единый гэп **4px** (divider центрируется по высоте: (40-28)/2=6px верт. инсет).
- Разделитель: ширина 1px, высота 28px, цвет `UI 04 #DBE7FD`(вероятно — светло-голубой, соответствует токену `UI 04` из variable_defs узла 40:573).
- Значки иконок на скрине — тёмные/чёрные (не акцентный синий), профиль — иконка+ФИО пользователя.

### 1.3 Workspace-вкладки (58:1107 «Tab», 58:1127 «Tab bar»)

Компонент «Tab» имеет 3×3 варианта (Property1 × Icon=No/Right/Left), размер базового варианта 167×36, паддинг текста `x=12,y=8` (лев. паддинг 12px, верт. центрирование). По скрину компонент-шита (tab-58-1107.png):

| Состояние               | Фон                            | Текст                     | Крестик                 |
| ----------------------- | ------------------------------ | ------------------------- | ----------------------- |
| Active (Icon=No)        | тёмный `UI 06 #222124`         | белый                     | нет                     |
| Active (Icon=Right)     | светло-голубой `UI 04 #DBE7FD` | тёмный                    | есть (×)                |
| default (Icon=No/Right) | белый `UI 01 #FFFFFF`          | тёмный `UI 06`            | нет / есть в Icon=Right |
| Hover (Icon=No/Right)   | белый `UI 01`                  | синий `Accent 02 #2A75F4` | нет / есть в Icon=Right |

Крестик закрытия явно показан только в варианте `Icon=Right` — то есть по спеке он не обязан быть всегда видимым.
`Tab bar` (58:1127, демо-полоса из 8 табов) целиком отрендерена вариантом «Active, Icon=No» (тёмные табы на ещё более тёмной подложке) — это, похоже, просто дефолтный пример компоновки, не буквальная демонстрация «все вкладки активны».
Гэп между табами в `Tab bar`: позиции инстансов идут с шагом 168px при ширине таба 167px → **гэп ≈1px** между вкладками.

### 1.4 Страница модуля (175:7122 «Отчёты», аналогично 150:4094 «Банк и касса»)

- Контентная зона `Body` 1508×1080, паддинг слева/сверху заголовка: заголовок (H2, 26/700) на `x=32,y=100` (в координатах Body).
- Сетка колонок (`Frame 35703`, `x=32,y=172,w=1436`): **3 равные колонки** по 462.67px, гэп между колонками **24px** (486.667-462.667). Число колонок в спеке фиксированное (Финансирование/Банк/… | Ведомости/Справочники | Отчёты — 3 колонки на скрине «Банк и касса»; в «Отчётах» — тоже 3 колонки: Регламентированные отчёты | Мемориальные ордера | Бухгалтерские отчёты).
- Внутри колонки — стек групп с вертикальным гэпом между группами **32px** (напр. `Финансирование` height=104, следующая группа `Банк` начинается на y=136 → 136-104=32).
- Заголовок группы (`H3`-подобный, 20/700 либо акцентный) → гэп до списка ссылок **32px** (список начинается на y=32 внутри группы).
- Строка-ссылка («Menu list» дочернего типа, 150:4581): 462.67×36, без ведущей иконки-контента (только hover-контролы: звёздочка «избранное» 28×28 слева при hover/выборе, стрелка-переход 28×28 справа), текст начинается `x=36` от начала строки, `y=8` (вертикально центр для 20px строки текста в 36px строке).

---

## §2. Код сейчас

Есть 2 параллельные реализации сайдбара с идентичными Tailwind-классами: `src/widgets/sidebar/ui/sidebar.tsx` (легаси/фолбэк, монтируется как `fallback` в `<ShellSidebarHost>`, `src/app/App.tsx:186`) и `src/features/sdui/ui/nodes/shell/sidebar-node.tsx` + `sidebar-link-item.tsx` (SDUI). Ниже — легаси-файл, значения совпадают.

### 2.1 Сайдбар — `src/widgets/sidebar/ui/sidebar.tsx`

```12
'flex w-full max-h-14 items-center gap-3 rounded-lg py-2 text-left text-base text-ui-01 transition-colors',
isCollapsed ? 'justify-center px-2' : 'justify-start pl-4',
isActive && 'bg-ui-01 text-ui-06',
!isActive && 'cursor-pointer hover:bg-ui-01/10'
```

- Контейнер `<aside>` (:38-42): развёрнутый `w-103 pl-15 pr-5` = **412px / 60px / 20px** — совпадает с Figma 1:1. Фон тёмный наследуется от обёртки `src/app/layout/layout.tsx:38` (`bg-ui-06`), не задан на самом `<aside>`.
- Строка пункта (:10-16): высота `max-h-14`=**56px** (де-факто вынуждена иконкой `min-h-10`, реально ≈56px, совпадает с Figma); паддинг `py-2`=8px верт., `pl-4`=**16px** гориз. (Figma: 20px — минус 4px); гэп иконка↔текст `gap-3`=**12px** (совпадает); радиус `rounded-lg`=**12px** (Figma — визуально «капсула», радиус заметно больше 12px).
- Иконка-обёртка (:18-22): `min-w-10 min-h-10 rounded-lg` = **40×40px**, радиус 12px; активная — `bg-accent-01` (лайм, совпадает); дефолтная — `bg-ui-06` (=фон сайдбара, невидима, совпадает с Figma).
- Сама иконка (:24-25): `w-6 h-6`=**24×24px** ВНУТРИ 40×40 обёртки → в Figma глиф занимает все 40×40 без доп. отступа. Разница: код рисует иконку заметно мельче, с «воздухом» вокруг.
- Активная строка (:14): `bg-ui-01 text-ui-06` — белый фон/тёмный текст (формально совпадает с Figma). Дефолтная (:15): прозрачный фон, `hover:bg-ui-01/10`.
- Логотип (:44-50): `mb-15`=60px низ. отступ, `gap-5 pl-5` (когда развёрнут) = гэп 20px / паддинг слева 20px; иконка `h-10 w-10`=40×40 (в Figma внутренняя иконка Logo — 40×40, текст начинается через ~20px — примерно совпадает).

### 2.2 Топбар — `src/widgets/top-bar/ui/top-bar.tsx` (+ `top-bar-user.tsx`, `toolbar-actions.ts`)

- Контейнер (:23-25): `<nav className="flex items-center bg-ui-01 rounded-md">` — белый фон, радиус 8px; отдельная высота не задана явно, определяется паддингом кнопок.
- Иконки-кнопки — через `src/shared/ui/buttons/button.tsx`, `variant="tertiary"`: паддинг `p-2.5`=10px, иконка `h-5 w-5`=20×20px → итоговая кнопка **40×40px** (совпадает с Figma).
- Цвет `tertiary`: `text-accent-02` (**синий `#2A75F4`**) — на скрине Figma иконки выглядят тёмными/чёрными, не синими. **Расхождение цвета.**
- Разделители (:29,35,46,59,63): `mx-1 h-5 w-px bg-ui-04` = margin 4px, высота 20px (Figma: 28px), ширина 1px, цвет `UI 04` — совпадает по цвету и ширине, высота меньше (20 vs 28).
- Гэп между элементами через `mx-1` (по 4px с каждой стороны разделителя, т.е. эффективно 4px между кнопкой и разделителем) — совпадает с Figma (4px).
- Профиль (`top-bar-user.tsx`): иконка `h-5 w-5` (20×20, `text-ui-03`), текст `Typography body2`, `mx-1 gap-2` (гэп 8px).

### 2.3 Workspace-вкладки — `src/widgets/workspace-tab-bar/ui/workspace-tab-item.tsx`, `workspace-tab-bar.tsx`

```31-33
className={`flex shrink-0 cursor-pointer items-center gap-1.5 rounded-t-md border-none py-2 pr-1.5 pl-3 ${
  isActive ? 'bg-ui-04' : 'bg-ui-01'
}`}
```

- Таб: паддинг `py-2 pl-3 pr-1.5` = 8/12/6px (Figma: text-inset 12px слева, верт. центр — левый паддинг совпадает, асимметрия справа под крестик — не противоречит спеке); радиус `rounded-t-md`=8px верх.
- **Активный = `bg-ui-04` (светло-голубой `#DBE7FD`)**, **дефолтный = `bg-ui-01` (белый)**. По компонент-шиту Figma ни одно из состояний Active/default/Hover не даёт именно эту пару цветов для «активная=голубая / неактивная=белая»: Figma Active(без иконки)=тёмный/белый текст, default=белый/тёмный текст, Hover=белый/синий текст. Ближайшее совпадение цвета `UI 04` — вариант `Active, Icon=Right` (голубой фон+тёмный текст+крестик), но в коде этот цвет закреплён за «активная вкладка» в целом, а не за конкретной вариацией с крестиком. **Однозначного соответствия состояний нет.**
- Крестик (:42-52): рендерится **всегда** (не по условию/hover), `opacity-60 hover:opacity-100`, `size-4`=16×16px. В Figma крестик визуально относится к варианту `Icon=Right`, т.е. по спеке это не безусловное поведение всех вкладок.
- Явного `hover:` для неактивной (не наведённой) вкладки в фоне НЕТ — то есть Hover-состояние из Figma (белый фон + синий текст) в коде не реализовано вовсе.
- Гэп между табами (`workspace-tab-bar.tsx:87`): `gap-px`=**1px** — совпадает с Figma (~1px по расчёту позиций).

### 2.4 Страница модуля — легаси `src/pages/module/ui/module-nav-list.tsx` + `module-nav-item.tsx` (SDUI-фолбэк путь; в чистом SDUI-пути колонки/гэп приходят с сервера через `grid-node.tsx`/`group-node.tsx`, `columns`/`gap` — серверные пропсы, дефолт 1 колонка/0 гэп)

```19-24 module-nav-list.tsx
<div className="grid gap-x-10" style={{ gridTemplateColumns: `repeat(${items.length}, auto)` }}>
  ...
  <div className="flex flex-col gap-6 justify-start">
```

- Число колонок = `items.length` (динамическое, по числу групп верхнего уровня), ширина колонок `auto` (по контенту). **Figma: фиксировано 3 равные колонки по 462.67px.** Если групп больше 3 — сетка в коде «расползается» вбок множеством узких колонок вместо переноса в 3 фиксированные колонки, как в макете.
- Гэп между колонками: `gap-x-10`=**40px** (Figma: 24px).
- Гэп между группами внутри колонки: `gap-6`=**24px** (Figma: 32px).
- Заголовок группы → список: `gap-2`=**8px** (Figma: 32px) — заметно теснее спеки.
- Гэп между строками-ссылками внутри группы: `gap-1`=**4px** (в Figma список идёт вплотную, элементы — отдельные компоненты по 36px без явного зазора между ними, т.е. Figma ближе к 0, код добавляет 4px — незначительно).
- Строка-ссылка (`module-nav-item.tsx:24-42`): без ведущей иконки (совпадает с Figma), `rounded-md hover:bg-ui-01`, звёздочка-избранное `h-4 w-4`=16×16 (показывается только при hover), стрелка `h-5 w-5`=20×20 (fade-in при hover) — по составу соответствует Figma.

---

## §3. Расхождения

| Что                                                                          | Сейчас (код)                                                                                                     | По Figma                                                                                                                                          | Где чинить                                                                                                                 | Приоритет                             |
| ---------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- | ------------------------------------- |
| Радиус активной строки сайдбара                                              | `rounded-lg`=12px (`sidebar.tsx:12`, `sidebar-link-item.tsx` аналог)                                             | визуально «капсула», радиус ≫12px (ближе к половине высоты строки, ~28px)                                                                         | `src/widgets/sidebar/ui/sidebar.tsx:12`, `src/features/sdui/ui/nodes/shell/sidebar-link-item.tsx`                          | P2                                    |
| Иконка пункта сайдбара мельче обёртки                                        | глиф `w-6 h-6`=24×24 внутри `min-w-10 min-h-10`=40×40 обёртки (`sidebar.tsx:24-25`)                              | иконка занимает весь квадрат 40×40 без внутреннего отступа                                                                                        | `sidebar.tsx:24-25` + SDUI-аналог                                                                                          | P2                                    |
| Левый паддинг строки сайдбара                                                | `pl-4`=16px (`sidebar.tsx:13`)                                                                                   | 20px (icon x=20 в Menu list)                                                                                                                      | `sidebar.tsx:13`                                                                                                           | P3                                    |
| Цвет иконок топбара                                                          | `text-accent-02` синий (`button.tsx:20-21`, tertiary-вариант)                                                    | тёмные/чёрные на скрине Figma, не синие                                                                                                           | `src/shared/ui/buttons/button.tsx:20-21` (либо отдельный вариант для топбар-кнопок)                                        | P2                                    |
| Высота разделителей топбара                                                  | `h-5`=20px (`top-bar.tsx:29,35,46,59,63`)                                                                        | 28px                                                                                                                                              | `src/widgets/top-bar/ui/top-bar.tsx`                                                                                       | P3                                    |
| Цвета состояний workspace-вкладок не соответствуют ни одной комбинации Figma | активная=`bg-ui-04` голубая, дефолтная=`bg-ui-01` белая; Hover не реализован (`workspace-tab-item.tsx:31-33,49`) | Active(без иконки)=тёмная/белый текст; default=белая/тёмный текст; Hover=белая/синий текст — активная НЕ голубая ни в одном варианте без крестика | `src/widgets/workspace-tab-bar/ui/workspace-tab-item.tsx:31-39`                                                            | P1                                    |
| Крестик закрытия вкладки всегда виден                                        | безусловный рендер (`workspace-tab-item.tsx:42-52`)                                                              | в компонент-шите крестик относится к варианту `Icon=Right`, т.е. не факт что должен быть на каждой вкладке всегда                                 | `workspace-tab-item.tsx:42-52`                                                                                             | P3 (нужно уточнение продукта/дизайна) |
| Сетка колонок страницы модуля не фиксирована                                 | `gridTemplateColumns: repeat(items.length, auto)`, гэп 40px (`module-nav-list.tsx:19-22`)                        | фиксированные 3 равные колонки по 462.67px, гэп 24px                                                                                              | `src/pages/module/ui/module-nav-list.tsx:19-22` (легаси-фолбэк; в SDUI-пути — серверные `columns`/`gap` в `grid-node.tsx`) | P1                                    |
| Гэп заголовок-группы → список ссылок                                         | `gap-2`=8px (`module-nav-list.tsx:26-36`)                                                                        | 32px                                                                                                                                              | `module-nav-list.tsx:31` (примерно, между Typography и `<ul>`)                                                             | P2                                    |
| Гэп между группами в колонке                                                 | `gap-6`=24px (`module-nav-list.tsx:24`)                                                                          | 32px                                                                                                                                              | `module-nav-list.tsx:24`                                                                                                   | P2                                    |
| «Тёмная плашка» активного пункта сайдбара с натуры (жалоба владельца)        | код задаёт `bg-ui-01` (fallback `#ffffff`, белый) — не воспроизводится статическим чтением кода                  | плашка должна быть белая                                                                                                                          | требует визуальной проверки на dev-стенде (браузер/дев-тулы), не выполнено в рамках этой выгрузки                          | P1 (для отдельной проверки)           |

Полные скрины и метаданные — в `/private/tmp/claude-501/-Users-buterbrot359-Development-MishaWeb-fin-web/4819f856-4d7e-442f-b097-9c8cb8000eaa/scratchpad/f3-audit/figma/`.
