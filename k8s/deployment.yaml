apiVersion: apps/v1
kind: Deployment
metadata:
  name: fin-web
  labels:
    app: fin-web
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fin-web
  template:
    metadata:
      labels:
        app: fin-web
    spec:
      containers:
      - name: fin-web
        image: DOCKER_IMAGE_PLACEHOLDER
        imagePullPolicy: Always
        env:
        - name: ANTHROPIC_API_KEY
          valueFrom:
            secretKeyRef:
              name: fin-web-secret
              key: ANTHROPIC_API_KEY
        ports:
        - containerPort: 4173
          name: web
        - containerPort: 3001
          name: mock
        resources:
          limits:
            cpu: "1"
            memory: "2Gi"
          requests:
            cpu: "500m"
            memory: "1Gi"
