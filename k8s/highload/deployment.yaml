apiVersion: apps/v1
kind: Deployment
metadata:
  name: fin-web
  namespace: highload
  labels:
    app: fin-web
    env: highload
  annotations:
    fin-web.io/deployed-commit: "${IMAGE_TAG}"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fin-web
  template:
    metadata:
      labels:
        app: fin-web
        env: highload
      annotations:
        fin-web.io/deployed-commit: "${IMAGE_TAG}"
    spec:
      # gitlab-registry — pull-secret для registry.qazyna.ai (создаёт deploy-job), ghcr-secret — GitHub-путь
      imagePullSecrets:
      - name: gitlab-registry
      - name: ghcr-secret
      containers:
      - name: fin-web
        image: ${IMAGE_NAME}
        imagePullPolicy: Always
        ports:
        - containerPort: 4173
          name: web
        - containerPort: 3001
          name: mock
        env:
        - name: ANTHROPIC_API_KEY
          valueFrom:
            secretKeyRef:
              name: fin-web-secret
              key: ANTHROPIC_API_KEY
        - name: DOCUMENT_TYPES_API_BASE_URL
          value: "https://highload-api.qazyna.ai"
        resources:
          limits:
            cpu: "1"
            memory: "1.5Gi"
          requests:
            cpu: "300m"
            memory: "512Mi"
