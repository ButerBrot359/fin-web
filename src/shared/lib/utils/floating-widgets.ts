/**
 * Отступ снизу для плавающих виджетов: кнопки поддержки, кнопки помощника и их окон.
 *
 * <p>Правый нижний угол занят не полностью: под ним лежит панель вкладок
 * (`widgets/workspace-tab-bar`) — строка высотой 36 px плюс 8 px отступа сверху,
 * прижатая к низу макета. Виджеты с `bottom-6` (24 px) вставали ПОВЕРХ неё и
 * закрывали крестик самой правой вкладки: её нельзя было закрыть, хотя она видна.
 *
 * <p>Значение с запасом: 64 px против ~44 px панели. Запас нужен потому, что панель
 * вкладок живёт в потоке, а виджеты — в `fixed`, и связать их точно можно было бы
 * только измерением в рантайме; лишние двадцать пикселей дешевле, чем перекрытый
 * крестик.
 *
 * <p>Одна константа на все четыре места намеренно: разъехавшиеся отступы у кнопки
 * поддержки и кнопки помощника — это две кнопки, стоящие не в колонку.
 */
export const FLOATING_BOTTOM = 'bottom-16'

/**
 * Второй этаж — кнопка помощника над кнопкой поддержки.
 *
 * <p>{@link FLOATING_BOTTOM} (64) + высота круглой кнопки (56) + просвет (8).
 * Рядом их не поставить: во время звонка кнопка поддержки подменяется панелью
 * звонка шириной 288 px по тем же координатам, и сосед слева оказался бы под ней.
 */
export const FLOATING_BOTTOM_SECOND = 'bottom-32'
