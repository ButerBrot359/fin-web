import type { AiStatistics } from '../../../src/entities/analytics/types/ai-statistics'

/** Synthetic data only for isolated browser tests; never included in production pages. */
export const aiStatisticsFixture = (): AiStatistics => ({
  from: '2026-08-05',
  to: '2026-09-03',
  groupBy: 'DAY',
  surface: 'ALL',
  timezone: 'Asia/Almaty',
  currency: 'USD',
  pricingCoveragePercent: 100,
  modelsTruncated: false,
  metrics: {
    requests: 1248,
    assistantRequests: 980,
    analyticsRequests: 268,
    successCount: 1223,
    errorCount: 25,
    successRate: 98,
    avgLatencyMs: 2840,
    p50LatencyMs: 2100,
    p95LatencyMs: 6320,
    inputTokens: 2840000,
    outputTokens: 624000,
    totalTokens: 3464000,
    avgTokens: 2775.64,
    estimatedCost: 18.42,
    avgEstimatedCost: 0.01476,
    pricedRequests: 1248,
    activeUsers: 24,
    conversations: 312,
    models: 3,
    actions: 186,
  },
  series: Array.from({ length: 30 }, (_, index) => ({
    bucket: new Date(Date.UTC(2026, 7, 5 + index)).toISOString().slice(0, 10),
    requests: 24 + ((index * 7) % 43),
    errorCount: index % 4,
    inputTokens: 24000 + ((index * 7321) % 120000),
    outputTokens: 11000 + ((index * 3214) % 34000),
    totalTokens: 35000 + ((index * 7321) % 120000) + ((index * 3214) % 34000),
    avgLatencyMs: 1600 + ((index * 237) % 3400),
    estimatedCost: 0.24 + ((index * 3) % 18) / 20,
  })),
  byModel: [
    {
      provider: 'OPENAI',
      model: 'Model A',
      requests: 780,
      errorCount: 14,
      inputTokens: 1800000,
      outputTokens: 380000,
      totalTokens: 2180000,
      avgLatencyMs: 2100,
      estimatedCost: 10.12,
    },
    {
      provider: 'ANTHROPIC',
      model: 'Model B',
      requests: 320,
      errorCount: 8,
      inputTokens: 800000,
      outputTokens: 180000,
      totalTokens: 980000,
      avgLatencyMs: 3200,
      estimatedCost: 6.5,
    },
    {
      provider: 'LOCAL',
      model: 'Model C',
      requests: 148,
      errorCount: 3,
      inputTokens: 240000,
      outputTokens: 64000,
      totalTokens: 304000,
      avgLatencyMs: 5600,
      estimatedCost: 1.8,
    },
  ],
  notes: [
    'REQUEST_UNITS_DIFFER',
    'COST_IS_CURRENT_RATE_ESTIMATE',
    'ACTIONS_ARE_AUDIT_ATTEMPTS',
  ],
})
